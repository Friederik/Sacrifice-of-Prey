/**
 * Исходный класс, позволяющий конвертировать всю игру в конфигурацию
 */
export default class JSONConvertible {
    /**
     * Создает строку JSON по всем полям и всем вложенностям
     * @returns Строка JSON
     */
    toJSON() {
        return "WIP";
    }
}
